    // check 'CRE guidelines' evaluations 1-9 (all CRE project types)
    var evalFields = [
        { id: "DropDown1", label: "No refinancing of other bank's exposures" },
        { id: "DropDown2", label: "No restructuring" },
        { id: "DropDown3", label: "No speculation" },
        { id: "DropDown4", label: "Personal guarantee as majority shareholder or exercise control" },
        { id: "DropDown5", label: "Repayment capacity" },
        { id: "DropDown6", label: "No negative risk indicator" },
        { id: "DropDown7", label: "Minimal coverage in risk sectors" },
        { id: "DropDown8", label: "No negative net-assets" },
        { id: "DropDown9", label: "No funding of VAT" }
    ];

    evalFields.forEach(function (item) {
        var field = document.getElementById(item.id);
        if (saveAndClose == false && !isTBD(field) && !isTabSwitch && field.value == "-1") {
            messages.push(item.label + " is required.");
            if (focusField == null) {
                focusField = field;
            }
        }
    });
